import 'package:bloc_app/home/data.dart';
import 'package:flutter/material.dart';

class tengah extends StatelessWidget {
  const tengah({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(child: data());
  }
}
